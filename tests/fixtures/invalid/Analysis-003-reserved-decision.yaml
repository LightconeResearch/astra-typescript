# Invalid: decision named 'options' clashes with the path keyword used
# in #decisions.<id>.options.<id> anchors.
id: bad_reserved_decision
name: Reserved decision name test
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric
decisions:
  options:
    id: options
    label: Reserved name
    rationale: should fail validation
    default: a
    options:
      a:
        label: A
